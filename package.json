{
  "name": "overland-sea",
  "version": "1.0.0",
  "private": true,
  "description": "Rail-first overland journey planner for Southeast Asia, drawn on a map",
  "type": "module",
  "scripts": {
    "build": "node tools/build.mjs && APP=1 node tools/build.mjs && node tools/build-pages.mjs",
    "test": "npm run build && node tools/smoke.mjs"
  }
}
